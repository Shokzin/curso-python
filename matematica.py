import math

# Funções built-in (funções pré-definidas) em Python

""" valores = [2, 5, 8, -1, 0, 11, 7, -6]
print(max(valores)) # Imprime o valor máximo da lista
print(min(valores)) # Imprime o valor mínimo da lista

a = -5
b = 4
print(abs(a)) # Imprime o valor absoluto de a
print(pow(b, 3)) # Imprime o valor de b elevado a 3

c = 2.789011
print(round(c, 2)) # Imprime o valor de c arredondado para 2 casas decimais

# --

x = 8
y = 100 """

raiz_quadrada = math.sqrt(x)
print(raiz_quadrada)